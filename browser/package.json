{
  "private": true,
  "workspaces": ["pkg/ceres", "pkg/browser"],
  "scripts": {
    "build-browser": "wasm-pack build --scope patract --out-dir pkg/browser",
    "build-ceres": "tsc -p ./pkg/ceres/tsconfig.json",
    "build": "yarn build-browser && yarn && yarn build-ceres"
  }
}

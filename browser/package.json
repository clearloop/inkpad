{
  "private": true,
  "workspaces": ["pkg/ceres", "pkg/browser"],
  "scripts": {
    "wasm-pack": "wasm-pack build --scope patract --out-dir pkg/browser",
    "build": "yarn wasm-pack && yarn && cd pkg/ceres && yarn build"
  }
}
